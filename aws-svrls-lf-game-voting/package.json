{
  "name": "aws-svrls-lf-game-voting",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npx rimraf dist/ lambda.zip && npx mkdirp dist && npx tsc && zip -rq lambda.zip node_modules dist"
  },
  "author": "Vijayaraaghavan Manoharan",
  "license": "ISC",
  "devDependencies": {
    "@aws-sdk/client-dynamodb": "^3.429.0",
    "@aws-sdk/lib-dynamodb": "^3.430.0",
    "@types/aws-lambda": "^8.10.125",
    "aws-lambda": "^1.0.7",
    "mkdirp": "^3.0.1",
    "rimraf": "^5.0.5",
    "serverless": "^3.35.2",
    "typescript": "^5.2.2"
  }
}
